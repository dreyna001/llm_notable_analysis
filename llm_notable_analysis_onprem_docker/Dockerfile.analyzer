FROM python:3.13-slim-bookworm

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    PIP_NO_CACHE_DIR=1

WORKDIR /app

RUN apt-get update \
    && apt-get install -y --no-install-recommends ca-certificates \
    && rm -rf /var/lib/apt/lists/*

COPY requirements.analyzer-docker.txt /tmp/requirements.txt
RUN pip install --upgrade pip \
    && pip install -r /tmp/requirements.txt

COPY onprem_service /app/onprem_service
COPY onprem_rag /app/onprem_rag

RUN useradd --create-home --home-dir /home/analyzer --shell /usr/sbin/nologin analyzer \
    && chown -R analyzer:analyzer /app

USER analyzer

ENTRYPOINT ["python", "-m", "onprem_service.onprem_main_nonsdk"]

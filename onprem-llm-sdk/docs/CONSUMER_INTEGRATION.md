# Consumer Integration

This document is intentionally thin to avoid duplication.

Use `docs/DEPLOY_AND_CONSUME.md` as the canonical guide for:

- offline installation from `tar.gz` bundle
- environment configuration
- application code integration
- error-handling patterns
- upgrade and rollback

